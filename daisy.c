#include <stdio.h>
#include <string.h>
int main(){
    char str[];
    int strlength;
    printf("enter the string");
    fgets(str,sizeof(str),stdin);
    strlength=strlen
    printf("the length is:");
    puts(str);
    return 0;
}